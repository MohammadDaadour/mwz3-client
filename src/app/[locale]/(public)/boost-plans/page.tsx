type Props = { params: { locale: string } };

export default async function BoostPlans({ params: { locale } }: Props) {
    return (
        <div>
            <div>
                alo
            </div>
        </div>
    )
}